apiVersion: v1
kind: Service
metadata:
  namespace: masterclass
  name:  [your-name]-app-service
spec:
  selector:
    app: [your-name]-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
